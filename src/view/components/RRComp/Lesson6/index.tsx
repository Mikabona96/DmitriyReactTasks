import React, { FC } from 'react';
import { Student } from '../../../../bus/student';

export const LessonSix: FC = () => {
    return (
        <div>
            <Student />
        </div>
    );
};
